<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <title><%= title %></title>
</head>
<body>
    <div>
        <h1><%= title %></h1>
        <a href="./">Back</a>
    </div>
    <% if (data[0].id !== null) { %>
        <%- include("partials/books",{books: data}) %>
    <% } else {%>
        <h3> No Books Found</h3>
        <a href="/books/new">Add a book</a>
    <%}%>
</body>
</html>